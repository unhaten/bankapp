export interface IUser {
    email: string;
    name: string;
    currency: number;
    // role: string;
    // _id: string;
    // createdAt: Date;
    // updatedAt: Date;
    // __v: number;
}

export interface IGenericResponse {
    status: string;
    message: string;
}
